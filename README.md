# تعليمات التشغيل

## 1. ضع الـ API Key بتاعك
افتح ملف bot.py وغير السطر ده:
ALIBABA_API_KEY = "your_alibaba_api_key"

## 2. ثبّت المكتبات
pip install -r requirements.txt

## 3. شغّل البوت
python bot.py
